#include<stdio.h>
int main(){
    // int x=5;
    // printf("%d\n", x);
    //  //x=x+1
    //  printf("%d\n", x++); // x++ means use x then increment
    //  //++x means increment then use

    // int x=4,y=3,z;
    // z=x-- -y;
    // printf("\n %d %d %d", x, y, z);

    // while('a'<'b')
    // printf("\nmalayalam is a palindrome");

    int i=10;
    while(i=20)
    printf("\nA Computer Buff!");
    return 0;
}